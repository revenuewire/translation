version: '3.8'
services:
  test:
      build: ./
      volumes:
          - $PWD:$PWD
      links:
          - redis
      command: ./vendor/bin/phpunit
      working_dir: $PWD
      environment:
        - GCT_KEY=$GCT_KEY
        - GCT_PROJECT=$GCT_PROJECT
  redis:
      image: redis:latest
